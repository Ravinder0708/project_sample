@model List<Vidly.Models.Movie>
@{
	ViewBag.Title = "Random";
	Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>Movies</h2>
@*@if (Model.Customers.Count == 0)
	{
		<text>No one rented for movie.</text>
	}
	else
	{
		<ul>
			@foreach (var item in Model.Movie.Name)
			{
				<li>Html.ActionLink(item, "Display", new { controller = "Movies" }, new { class = "nav-link" })</li>

			}
		</ul>
	}*@
<ul>
	@foreach (var item in Model)
	{
		@*<li>@Html.ActionLink(item.Name, "Display", new { controller = "Movies" }, new { @class = "nav-link" })</li>*@
		<li>@Html.ActionLink(@item.Name, "Display", "Movies", new { id = @item.Id }, new { @class = "nav-link" })</li>

	}
</ul>
